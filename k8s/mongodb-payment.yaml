apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-payment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-payment
  template:
    metadata:
      labels:
        app: mongodb-payment
    spec:
      containers:
      - name: mongodb
        image: mongo:latest
        ports:
        - containerPort: 27017

---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-payment
spec:
  selector:
    app: mongodb-payment
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
